node_modules
dist

# Regenerated by `prisma generate`, which `npm run build` runs first.
prisma/generated

# Keep environment variables out of version control.
# The glob matters: `vercel env pull` writes .env.local, which a bare
# `.env` rule would not catch.
.env
.env*

.vercel
